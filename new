pipeline{
    agent any
    stages{
        stage("git checkout"){
            steps{
               git branch: 'new', credentialsId: 'github', url: 'https://github.com/Santhoskum/bangalore_demo.git'
            }
        }
    }
}
